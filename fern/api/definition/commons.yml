# yaml-language-server: $schema=https://raw.githubusercontent.com/fern-api/fern/main/fern.schema.json

types:
  DataType:
    enum:
      - text
      - categorical
      - multi_categorical
      - quantitative
      - character_offsets
      - url
      - ordinal_regression
      - file_pdf
